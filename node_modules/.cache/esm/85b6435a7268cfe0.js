let axios,cheerio;_726‍.x([["scrapeHTML",()=>scrapeHTML],["cheerioArray",()=>cheerioArray],["generateObject",()=>generateObject]]);_726‍.w("axios",[["default",["axios"],function(v){axios=v}]]);_726‍.w("cheerio",[["default",["cheerio"],function(v){cheerio=v}]]);


async function scrapeHTML(url) {
  const { data: html } = await axios.get(url);
  return html;
}

function cheerioArray(html, query) {
  const $ = cheerio.load(html);
  const list = $(query);
  const array = [...list];
  return array;
}

function generateObject(array) {
  const list = [];
  for (let i = 0; i < array.length; i++) {
    const object = {
      name: array[i].attribs.title,
      id: array[i].attribs.id
    };
    list.push(object);
  }
  _726‍.g.console.log(list);
}

// console.log(array[0].attribs.title);

